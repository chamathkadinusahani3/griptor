# Copy to .env.local (already gitignored via the *.local rule) and adjust if needed.

# Origin (protocol + host + port) where the griptoradmin app + API run.
# Local dev default assumes `npm run dev:full` (vercel dev) in griptoradmin on port 3000.
VITE_ADMIN_ORIGIN=http://localhost:3000
